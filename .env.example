# Frontend (Vite)
# Point to the API origin. If server runs on 8787 locally:
VITE_API_BASE=http://localhost:8787

# Server
SHEETS_SPREADSHEET_ID=
SHEETS_WORKSHEET_NAME=Sheet1
# Absolute path to your service account JSON key file
GOOGLE_APPLICATION_CREDENTIALS=
PORT=8787
# Force modal for testing (1 = always show)
VITE_FORCE_VISITOR_MODAL=
